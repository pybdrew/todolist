version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto-17
    commands:
      - echo "Installing Maven"
      - mvn --version
  build:
    commands:
      - echo "Starting Maven build"
      - cd todo
      - mvn -B package
artifacts:
  files:
    - todo/target/*.jar
